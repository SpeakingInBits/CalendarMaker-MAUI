<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CalendarMaker_MAUI.Views.ProjectsPage"
             Title="Projects">
    <VerticalStackLayout Padding="16" Spacing="12">
        <Button Text="Create Default Project" x:Name="CreateBtn" />
        <CollectionView x:Name="ProjectsList" SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="8" Margin="0,6" HasShadow="False" BorderColor="LightGray">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontAttributes="Bold" />
                                <Label Text="{Binding Year}" />
                            </VerticalStackLayout>
                            <HorizontalStackLayout Grid.Column="1" Spacing="6">
                                <Button Text="Open" CommandParameter="{Binding .}" Clicked="OnOpenClicked" />
                                <Button Text="Delete" CommandParameter="{Binding .}" Clicked="OnDeleteClicked" />
                            </HorizontalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>