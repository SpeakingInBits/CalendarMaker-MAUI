<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shapes="clr-namespace:Microsoft.Maui.Controls.Shapes;assembly=Microsoft.Maui.Controls"
             x:Class="CalendarMaker_MAUI.Views.DesignerPage"
             Title="Designer">
    <Grid RowDefinitions="*" Padding="12" ColumnDefinitions="260,*" ColumnSpacing="12">
        <ScrollView Grid.Column="0" VerticalScrollBarVisibility="Always" Padding="0,0,10,0">
            <VerticalStackLayout Spacing="10">
                <Label Text="Designer" FontAttributes="Bold" />
                <HorizontalStackLayout Spacing="6">
                    <Button Text="Prev" x:Name="PrevBtn" />
                    <Label x:Name="MonthLabel" VerticalTextAlignment="Center" />
                    <Button Text="Next" x:Name="NextBtn" />
                </HorizontalStackLayout>

                <Button Text="Add Photo" x:Name="AddPhotoBtn" />

                <BoxView HeightRequest="1" Color="#DDD" />
                <Label Text="Layout" FontAttributes="Bold" />
                <Button Text="Flip Layout" x:Name="FlipBtn" />
                <!-- Split slider with reset -->
                <Grid x:Name="SplitControlGrid" ColumnDefinitions="Auto,*,Auto,Auto" RowDefinitions="Auto">
                    <Label Text="Split" />
                    <Slider x:Name="SplitSlider" Grid.Column="1" Minimum="0.3" Maximum="0.7" />
                    <Label x:Name="SplitValueLabel" Grid.Column="2" />
                    <Border x:Name="SplitResetBtn" Grid.Column="3"
                            WidthRequest="36" HeightRequest="36"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                            Padding="6" Stroke="Transparent" StrokeThickness="0"
                            StrokeShape="RoundRectangle 8,8,8,8">
                        <Grid>
                            <shapes:Path Stroke="White" StrokeThickness="2" Data="M23 4 L23 10 L17 10" />
                            <shapes:Path Stroke="White" StrokeThickness="2" Data="M20.49 15 A9 9 0 1 1 22.62 6" />
                        </Grid>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnSplitResetTapped" />
                        </Border.GestureRecognizers>
                    </Border>
                </Grid>
                <!-- Photo layout picker -->
                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto">
                    <Label Text="Photo Layout" />
                    <Picker x:Name="PhotoLayoutPicker" Grid.Column="1" HorizontalOptions="FillAndExpand" >
                        <Picker.Items>
                            <x:String>Single</x:String>
                            <x:String>Two (vertical split)</x:String>
                            <x:String>Grid 2x2 (4)</x:String>
                            <x:String>Two (stacked)</x:String>
                            <x:String>Three (2 left, 1 right)</x:String>
                            <x:String>Three (1 left, 2 right)</x:String>
                        </Picker.Items>
                    </Picker>
                </Grid>
                <!-- Zoom slider with reset -->
                <Grid ColumnDefinitions="Auto,*,Auto,Auto" RowDefinitions="Auto">
                    <Label Text="Photo Zoom" />
                    <Slider x:Name="ZoomSlider" Grid.Column="1" Minimum="0.5" Maximum="3.0" Value="1" />
                    <Label x:Name="ZoomValueLabel" Grid.Column="2" />
                    <Border x:Name="ZoomResetBtn" Grid.Column="3"
                            WidthRequest="36" HeightRequest="36"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                            Padding="6" Stroke="Transparent" StrokeThickness="0"
                            StrokeShape="RoundRectangle 8,8,8,8">
                        <Grid>
                            <shapes:Path Stroke="White" StrokeThickness="2" Data="M23 4 L23 10 L17 10" />
                            <shapes:Path Stroke="White" StrokeThickness="2" Data="M20.49 15 A9 9 0 1 1 22.62 6" />
                        </Grid>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnZoomResetTapped" />
                        </Border.GestureRecognizers>
                    </Border>
                </Grid>

                <BoxView HeightRequest="1" Color="#DDD" />
                <Label Text="Calendar" FontAttributes="Bold" />
                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto">
                    <Label Text="Year" />
                    <Entry x:Name="YearEntry" Grid.Column="1" Keyboard="Numeric" Placeholder="e.g. 2024" />
                </Grid>
                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto">
                    <Label Text="Start Month" />
                    <Picker x:Name="StartMonthPicker" Grid.Column="1"/>
                </Grid>
                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto">
                    <Label Text="First Day of Week" />
                    <Picker x:Name="FirstDowPicker" Grid.Column="1"/>
                </Grid>

                <BoxView HeightRequest="1" Color="#DDD" />
                <Label Text="Export" FontAttributes="Bold" />
                <Button Text="Export Month (PDF)" x:Name="ExportBtn" />
                <Button Text="Export Cover (PDF)" x:Name="ExportCoverBtn" />
                <Button Text="Export Full Year (PDF)" x:Name="ExportYearBtn" />
                <Button Text="Back" x:Name="BackBtn" />
            </VerticalStackLayout>
        </ScrollView>
        <Border Grid.Column="1" Stroke="LightGray" StrokeThickness="1" Padding="8">
            <ContentView x:Name="CanvasHost" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
        </Border>
    </Grid>
</ContentPage>